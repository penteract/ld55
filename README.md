A game in which you summon and can be summoned by various demons.


`game.py` The game logic

`main.py` starts an HTTP server on port 8090. 

Entirely single threaded; makes no attempt to persist state.
